language: cpp
compiler:
  - clang
  - gcc

install: true
before_script:
  - sudo apt-get update
  - sudo apt-get install libgtest-dev

script: mkdir build && cd build && cmake ../ && make && make test